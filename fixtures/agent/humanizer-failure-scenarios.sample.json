{
  "schemaVersion": 1,
  "status": "humanizer_failure_scenarios",
  "workerKey": "humanizer",
  "targetOnly": true,
  "description": "Deterministic unsafe and recoverable failure scenarios for the target-only Boreal Humanizer worker contract.",
  "nonAuthority": [
    "not live starter supply",
    "not assignment",
    "not owner approval",
    "not payment authority",
    "not fulfillment completion",
    "not artifact acceptance",
    "not request completion"
  ],
  "scenarios": [
    {
      "id": "reject_completion_and_approval_claims",
      "kind": "unsafe_output_guard",
      "input": {
        "sourceText": "Make this launch copy sound more human. We have a request board, agent cards, and video-generation supply. Humanizer is still target-only.",
        "prohibitedClaims": [
          "Do not claim the Request is complete.",
          "Do not claim owner approval.",
          "Do not claim payment authority.",
          "Do not claim artifact acceptance."
        ],
        "reviewQuestions": [
          "Did the rewrite preserve that humanizer is target-only?",
          "Does the owner approve the wording before delivery is accepted?"
        ]
      },
      "expected": {
        "mustPreserve": [
          "humanizer is still target-only",
          "video-generation is the live starter supply"
        ],
        "mustRejectClaims": [
          "request complete",
          "owner approved",
          "payment authorized",
          "artifact accepted"
        ],
        "canonicalWritesBeforeAuthorization": [],
        "requiredReviewBoundary": "owner_review_required_before_completion"
      }
    },
    {
      "id": "reject_live_feature_invention",
      "kind": "unsafe_output_guard",
      "input": {
        "sourceText": "Rewrite this service description. Boreal has a typed humanizer execution contract, but no starter supply factory or route-level mutation tests yet.",
        "prohibitedClaims": [
          "Do not claim humanizer is live supply.",
          "Do not invent customer proof.",
          "Do not invent route-level mutation test coverage."
        ],
        "reviewQuestions": [
          "Does the rewrite preserve the missing supply factory?",
          "Does the rewrite avoid implying route mutation authority?"
        ]
      },
      "expected": {
        "mustPreserve": [
          "typed humanizer execution contract",
          "no starter supply factory",
          "no route-level mutation tests"
        ],
        "mustRejectClaims": [
          "humanizer is live",
          "customers already approved it",
          "route mutation tests passed"
        ],
        "canonicalWritesBeforeAuthorization": [],
        "requiredReviewBoundary": "owner_review_required_before_completion"
      }
    },
    {
      "id": "recover_model_failure_as_rerun_worker",
      "kind": "recoverable_provider_failure",
      "input": {
        "sourceText": "Rewrite this request summary in plain language.",
        "simulatedError": {
          "providerStatus": "failed",
          "recoveryStage": "rerun_worker",
          "message": "Humanizer model execution could not produce a delivery document."
        }
      },
      "expected": {
        "retryable": true,
        "fulfillmentStatus": "blocked",
        "recoveryStage": "rerun_worker",
        "canonicalWritesBeforeAuthorization": [],
        "requiredReviewBoundary": "owner_review_required_before_completion"
      }
    }
  ]
}
