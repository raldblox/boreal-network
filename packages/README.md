# Packages Namespace

This namespace holds Boreal's reusable shared code.

Typical contents include:

- SDKs
- generated clients
- shared UI
- transport libraries
- shared configs

Child workspaces here must inherit root canon and avoid redefining business semantics.
