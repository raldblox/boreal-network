# required
# generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=****

# required
# neon postgres connection string
POSTGRES_URL=****

# preferred direct OpenAI execution for OpenAI-backed model routes
# model routes try OPENAI_API_KEY first, then Vercel AI Gateway
OPENAI_API_KEY=****
BOREAL_REUSABLE_PROMPT_OPENAI_MODEL=gpt-5.4-nano

# optional fallback path when direct OpenAI is unavailable
AI_GATEWAY_API_KEY=****

# optional chat model routing thresholds
# default nano chats promote to gpt-5.4-mini for larger contexts before rotating through o3-mini, o4-mini, gpt-5-mini, and gpt-4.1-nano
BOREAL_CONTEXT_HEAVY_TOKEN_ESTIMATE=12000
BOREAL_CONTEXT_HEAVY_MESSAGE_COUNT=14
BOREAL_CONTEXT_HEAVY_ACTIVITY_COUNT=6

# optional Promptfoo local eval fallback
# keep 0 by default; set to 1 only when live Neon auth/db is unreachable and you need prompt/tool scoring without durable writes
BOREAL_PROMPTFOO_EVAL_NO_DB=0

# optional until boreal web needs file uploads or blob persistence
BLOB_READ_WRITE_TOKEN=****

# optional until Boreal-managed Runway supplies are enabled
RUNWAYML_API_SECRET=****
RUNWAYML_API_VERSION=2024-11-06

# optional until rate limiting or ephemeral coordination is enabled
# upstash or another redis-compatible provider can supply this
REDIS_URL=****

# optional reusable prompt chat-fork launch policy
# chats stay free; these limits protect live model capacity
BOREAL_REUSABLE_PROMPT_FREE_RUNS_ENABLED=true
BOREAL_REUSABLE_PROMPT_FREE_DAILY_CHAT_LIMIT=10
BOREAL_REUSABLE_PROMPT_TOPUP_DAILY_CHAT_LIMIT=20
BOREAL_REUSABLE_PROMPT_FREE_DAILY_TOKEN_LIMIT=20000
BOREAL_REUSABLE_PROMPT_TOPUP_DAILY_TOKEN_LIMIT=40000
BOREAL_REUSABLE_PROMPT_FREE_MAX_INPUT_TOKENS=4000
BOREAL_REUSABLE_PROMPT_FREE_MAX_OUTPUT_TOKENS=1200

# required for production WebAuthn MFA origin hardening
# local development may use http://localhost:3000 and rp id localhost
# use the actual apps/web Vercel production origin or custom alias
NEXT_PUBLIC_APP_URL=https://network.boreal.work
WEBAUTHN_ORIGIN=https://network.boreal.work
WEBAUTHN_RP_ID=network.boreal.work
WEBAUTHN_ALLOWED_ORIGINS=https://network.boreal.work

# required before problem-intel promotion writes are enabled
# generate a random operator-only token and keep it server-side only
PROBLEM_INTEL_EDIT_TOKEN=****
