# Boreal Peer Guidelines

## Scope

This `AGENTS.md` applies to `C:\Users\raldb\boreal-network\apps\peer`.

## Rules

- keep this workspace focused on runnable peer-host behavior
- treat peer transport as transport only, not Boreal request truth
- runtime identity must stay separate from Boreal actor identity
- shared transport helpers belong in `packages/network-*`
